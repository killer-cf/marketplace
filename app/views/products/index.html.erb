<section>
  <div>
    <% @products.each do |product| %>
      <div>
        <h2><%= product.name %> <%= product.brand %></h2>
        <p><%= product.description %></p>
        <p><%= product.price %></p>
      </div>
    <% end %>
  </div>
</section>